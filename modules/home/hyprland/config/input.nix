{ ... }:

# Hyprland input config
{
  wayland.windowManager.hyprland = {
    input = {
      kb_layout = "fr";
      numlock_by_default = true;
      follow_mouse = 1;
      sensitivity = 0;
      touchpad = {
        natural_scroll = true;
      };
    };
  };
}
